import 'package:mubrambl/src/HD/keygen.dart';
import 'package:test/test.dart';

void main() {
  
    test('derive xpub soft', () {
      final derivation_index = 128;
      final seed = generateSeed(entropy, password)
    }
}