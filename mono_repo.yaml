# Enabled GitHub actions -- https://docs.github.com/actions
github:
    # Setting just `cron` is a shortcut to keep the defaults for `push` and `pull_request` while adding a single `schedule` entry.
    cron: '0 0 * * 0' # "At 00:00 UTC on Sunday."
    